name: keep-alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -fsS https://<あなたのサービス名>.onrender.com/healthz || true
